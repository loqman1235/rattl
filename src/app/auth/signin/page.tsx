import { SignInModal } from "@/components/modals/signin-modal";

export const metadata = {
  title: "Sign in to Rattl.",
};

const SignInPage = () => {
  return <SignInModal />;
};
export default SignInPage;
